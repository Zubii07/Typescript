// Collection of key and value pairs is called object

// Define all types in top that we use in ana object eg:
type objType= {name:string; age:number; email:string }

const person = {  
    name: "AHmad",
    age: 21
}

console.warn(person.name);

const User = {  
    name: "Ali",
    age: 21
}

console.warn(User.age);
